# Fonts

Place **LTSuperior Bold** here for the hero typewriter:

- `LTSuperior-Bold.woff2` (preferred), or
- `LTSuperior-Bold.woff`, or
- `LTSuperior-Bold.ttf`

The app will use the first available format. If no file is present, the hero falls back to the default sans-serif.
